# attempts/ — candidate flag lists submitted to the live validator

Every list of strings we submitted to the SSH `flag:` oracle (via `../arcus_pty.py batch`).
All rejected. Grouped by hypothesis: `_arcus` (arcus{} wrapper), `_bare`, `_theme`/`_t2`/`_t3`
(poem themes), `_history` (Pessoa/Campos facts), `_probe` (validator edge-cases), `_poem` slugs.
The exhaustive run-time skip-set is tracked separately (`../brute_tried.txt`, gitignored).
